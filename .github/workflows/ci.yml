name: CI

on:
  pull_request:
    branches: [main]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Build web image
        uses: docker/build-push-action@v6
        with:
          context: ./infra/web
          push: false
